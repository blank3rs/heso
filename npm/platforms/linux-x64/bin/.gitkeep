# heso (Linux x86_64) is populated here at release time by
# .github/workflows/pypi.yml's npm-publish job (or scripts/release.ps1
# for emergency manual publishes). This file keeps the directory
# tracked in git so the publish step never has to mkdir -p.
