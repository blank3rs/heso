# heso (macOS aarch64 / Apple Silicon) is populated here at release
# time by .github/workflows/pypi.yml's npm-publish job. This file
# keeps the directory tracked in git so the publish step never has
# to mkdir -p.
